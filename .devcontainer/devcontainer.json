{
  "name": "spotify-dash",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",
  "forwardPorts": [8050],
  "postCreateCommand": "python -m venv .venv && . .venv/bin/activate && pip install -U pip && pip install -r requirements.txt || true",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "ms-toolsai.jupyter"]
    }
  }
}